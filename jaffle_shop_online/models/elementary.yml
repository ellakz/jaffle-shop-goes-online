version: 2
models:
  - name: order_items
    columns:
      - name: order_id
        data_tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
      - name: customer_id
        data_tests:
          - unique:
              config:
                severity: error
              meta:
                description: The column should contain only unique values
