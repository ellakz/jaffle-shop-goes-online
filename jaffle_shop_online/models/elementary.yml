version: 2
models:
  - name: order_items
    columns:
      - name: order_id
        data_tests:
          - dbt_utils.sequential_values:
              interval: 1 # The gap between two sequential values
      - name: customer_id
        data_tests:
          - dbt_utils.sequential_values:
              interval: 1 # The gap between two sequential values
