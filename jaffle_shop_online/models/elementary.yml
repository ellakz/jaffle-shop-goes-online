version: 2
models:
  - name: order_items
    columns:
      - name: order_id
        tests:
          - unique:
              config:
                error_if: '>10'
                warn_if: '>0'
              meta:
                owner:
                  - Maayan
                description: The column should contain only unique values
      - name: customer_id
        tests:
          - unique:
              config:
                error_if: '>10'
                warn_if: '>0'
              meta:
                owner:
                  - Maayan
                description: The column should contain only unique values
