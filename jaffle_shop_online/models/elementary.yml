version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        tests:
          - unique:
              config:
                tags:
                  - stability
                error_if: '>5'
              meta:
                owner:
                  - itamar+12@elementary-data.com
                description: The column should contain only unique values, fails if
                  more than 5 are not unique
      - name: order_id
        tests:
          - unique:
              config:
                tags:
                  - stability
                error_if: '>5'
              meta:
                owner:
                  - itamar+12@elementary-data.com
                description: The column should contain only unique values, fails if
                  more than 5 are not unique
