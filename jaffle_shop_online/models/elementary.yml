version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        tests:
          - unique:
              config:
                tags:
                  - stability
                error_if: '>5'
              meta:
                description: The column should contain only unique values
      - name: order_id
        tests:
          - unique:
              config:
                tags:
                  - stability
                error_if: '>5'
              meta:
                description: The column should contain only unique values
